function areaCirculo(raio) {
    const pi = Math.PI; // Valor de pi
    return pi * Math.pow(raio, 2); // Fórmula da área do círculo
}

// Exemplo de uso:
console.log(areaCirculo(5).toFixed(2)); // Exemplo: Área de um círculo com raio 5, com 2 casas decimais